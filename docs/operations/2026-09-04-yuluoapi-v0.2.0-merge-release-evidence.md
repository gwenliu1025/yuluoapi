# 雨落 API v0.2.0 汇流与发布证据

> 日期：2026-09-04（Asia/Shanghai）
>
> 范围：代码汇流、品牌前端保留、运维 Skill 沉淀、GitHub Release/GHCR 资产；不执行生产 Prepare、Activate、部署或重启。

## 1. 输入与边界

- 雨落基线：`main` / `v0.1.180`，提交 `7e61ba06b272b20901a2465831be69ed72c2fbd5`。
- 已验证旧站 fork：`legacy/release/v0.1.179-fork`，提交 `50e115bd54596f538a8eb5ae3659e664a5cbc64e`。
- 官方 `v0.2.0`：`aa236488351eb71e120fc2b6fb32e36b0374c918`，为上述 fork 合入提交的第二父提交。
- 固定保留：雨落品牌首页、首页素材和测试、GSAP 依赖、雨落仓库/GHCR/在线更新契约、Antigravity OAuth 环境变量注入、Docker updater 与发布门禁。
- 不变标签：`v0.1.179` 继续指向 `92f353939ad50946cb709a92cf7568d673aa0924`；`v0.1.180` 和 `design-home-v1` 继续指向 `7e61ba06b272b20901a2465831be69ed72c2fbd5`。

## 2. 冲突处理

- 后端冲突采用已验证的 `v0.2.0` fork 实现；雨落特有的更新仓库、镜像仓库和 OAuth 配置由项目现有 owner 保留。
- `HomeView.vue` 保留雨落默认首页分支，继续遵守 `home_content` → `compact_home_enabled` → `YuluoBrandHome` 的优先级。
- `frontend/package.json` 采用目标版本依赖并保留 `gsap 3.13.0`，随后重新生成并冻结 lockfile。
- 部署文件、Release workflow、VersionBadge 和发布门禁统一到 `gwenliu1025/yuluoapi` 与 `ghcr.io/gwenliu1025/yuluoapi:0.2.0`。
- 修复一处项目身份测试数据：Docker updater 缓存测试由旧仓库名改为雨落仓库名；生产逻辑未放宽。

## 3. 运维 Skill

新增 `.codex/skills/yuluoapi-ops/`：

- 只保留版本同步、Release/GHCR、Docker 更新、备份/预热/回滚、生产核验和 502/计费排查能力。
- 删除其他站点、CPA、QQ 机器人、具体主机 IP、账号、邮箱来源、备份实例和旧环境事实。
- 雨落固定信息为：本地仓库、GitHub 仓库、GHCR 仓库、`/opt/yuluoapi`、updater Socket 和公网域名；凭据只允许从批准的安全来源读取。
- `python -X utf8 .../quick_validate.py .codex/skills/yuluoapi-ops`：`Skill is valid!`。

## 4. 本地验证

| 验证 | 结果 |
|---|---|
| Go 1.27 unit | 通过；首次发现旧仓库名测试数据导致缓存未命中，修正后完整复验通过 |
| Go 1.27 integration | 通过 |
| golangci-lint 2.13.0 | `0 issues.` |
| 前端依赖 | `pnpm install --frozen-lockfile` 通过 |
| 前端 Lint / typecheck | 通过 |
| 前端全量测试 | `255 passed`，`1850 passed` |
| 前端生产构建 | 通过；产物含雨落首页图片、花瓣、雨滴与水波资源 |
| 发布版本与门禁脚本 | 通过 |
| Docker Compose security / Gateway env / runtime resources | 通过 |
| Caddy cache / SSE / compression | 通过 |
| install GitHub token checks | 通过 |
| `docker compose config --quiet` | 通过 |
| Docker 本地镜像 | `yuluoapi:0.2.0-local` 构建通过，容器输出应用版本 `0.2.0` |
| Apple container lifecycle | 本机未成立：该 macOS 专用测试使用 BSD `stat -f`，Windows Git Bash 提供 GNU `stat`；交由 GitHub macOS CI 验证 |
| `git diff --check` | 通过 |

## 5. 仓库发布

本节在远端发布完成后回写以下权威结果：

- 发布提交与 `origin/main`；
- annotated `v0.2.0` peeled commit；
- CI、Security Scan、Release workflow；
- Release 资产集合与 SHA-256；
- GHCR OCI index digest 和 `linux/amd64`、`linux/arm64` 清单；
- 远端镜像版本、提交和 OCI source。

## 6. 生产边界

本轮不连接生产主机，不调用 updater Prepare/Activate，不修改 `/opt/yuluoapi`，不重建或重启任何生产容器。生产继续使用现有 `v0.1.180`，由用户后续自行触发更新。
